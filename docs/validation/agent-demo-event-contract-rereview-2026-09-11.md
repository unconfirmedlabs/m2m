# Shared demo event helper: repaired-hash Astra re-review

Checked 2026-09-11 by Astra `xhigh`, against sections 1–9 of
[AGENT_DEMO_EVENTS_SPEC](../AGENT_DEMO_EVENTS_SPEC.md), the
[original E1–E7 review](agent-demo-event-contract-review-2026-09-11.md), actual
BudgetLedger and native public codec semantics. Implementation and owner tests
were read only. No credentials, external service/model calls, chain transactions,
provisioning or deployment occurred. Browser execution used explicitly labeled
injected fixtures, not the production UI.

**Result: return for five narrow corrections before installation in L1/L2/UI.**
Most exact original failures are repaired. The new accounting and detached-config
behavior is independently supported; this is not a blanket acceptance of all
public boundaries.

## Frozen provenance and commands

Helper hashes matched before and after all probes:

| File under `scripts/` | SHA-256 |
|---|---|
| `agent-demo-event-contract.ts` | `c89ff29447258d9f89ff7a8cd09b28ced14343112821b221b5ddad5d940ccd7b` |
| `test-agent-demo-event-contract.ts` | `82c639fb25597b83c819cc29f2935af83f21c4ce869ea6e2d41b0367e573c436` |
| Root browser test after adding the final-page regression | `584eb1d204d031e396de7e416658316693cd2fe6156953768bb9439598c0361b` |

Passed independently:

```sh
./node_modules/.bin/tsx scripts/test-agent-demo-event-contract.ts
./node_modules/.bin/tsx scripts/test-agent-demo-public-fixture.ts
./node_modules/.bin/tsx scripts/test-agent-demo-economic-session.ts
```

`npm run agent-demo-contract-browser-tests` first passed its original six
regressions. Root then added ER-01 below to the root-owned browser script; a
second independent execution fails **only that seventh assertion**. All six
earlier assertions still pass. The actual Chromium bundle is 60,801 bytes with
exactly one runtime input, `scripts/agent-demo-event-contract.ts`, no external
runtime modules, and `Buffer`/`process` both undefined. All 19 coherent public
fixture events and canonical cursor roundtrip pass in that browser. No UI
installation or full HTTP/SSE reducer behavior was exercised by this test.

## Independently established repaired behavior

The previous 39-case source corpus now accepts **39/39 allowed variants**, with
zero wrong-source acceptances among its applicable source reversals. It includes
all original tool-result families, research/follow_up, web tools, receipt outcome
shapes, source errors and all six additive lifecycle names. Runtime events use
their actual pre-append host cursor, not the final snapshot cursor. This is a
variant corpus, not a complete state/provenance Cartesian product or signature
verification of its deliberately synthetic mutants.

Exact original negative re-probes now reject:

- Provider operator.task/operator.status; arbitrary operator status state;
  session/role/receipt enum arrays.
- Nonfinal channel_final, incorrect credit price and U128 price overflow;
  receipt discarded bytes above generated bytes; wrong fixed funding deposit;
  runtime cursor including its own host event.
- Missing/older valid checkpoint with declared delivery 26, invented unobserved
  refund, redeemed above deposit, duplicate channels, equal current economic and
  transport keys, mismatched provider network and zero budget turn limit.
- Wrong funding-control configuration pin, surrogate object keys, direct control
  and canonical-array getters, escaped duplicate JSON keys, prototype-sensitive
  parser names, sparse arrays and array extra properties through canonical JSON.

Positive checks preserve valid astral Unicode, a generic 32,769-character JSON
string, all nine command shapes, a safe citation and rejection of its added body
field/ordinal gap. `backend_unavailable` is accepted for the coordinator fallback.
A codec-valid offer with refund==payee is accepted; no recipient-distinctness
rule was introduced. Mutating the validated config no longer changes the input
snapshot or pins, and changing the current economic key while preserving the
historical offer remains accepted.

### Actual BudgetLedger reservation, uncertainty and reopen

The owner/browser tests use handwritten reserved values. This independent probe
instead opened a real temporary BudgetLedger, reserved/bound the fixture funding,
began its request and replayed its credit reservations/checkpoint observations.
Before completing the request it called `reserveCredit` with ceilings `[1,16]`,
the ledger's actual delivered units and original `[0,0]` request baseline, then
called `markUncertain`. No additional signed credit was created.

Replacing only the public economy's budget with `ledger.snapshot()` and recording
the reserved maximum/exposure produces:

| Field | Actual accepted value |
|---|---:|
| Reserved maximum | 50 |
| Historical signed maximum | 38 |
| Delivered price | 26 |
| Reserved exposure | 24 |
| Signed exposure | 12 |
| Budget uncertainty | true |

Validation passes before and after closing/reopening the actual ledger. This
supports E2's distinction without treating an unsigned reservation as a signature.

A second real ledger probe bound funding but created no request/credit, then
applied an explicitly injected terminal refunded observation with zero units and
zero redeemed. The resulting released budget validates in a public snapshot with
`signed_credit:null`, `checkpoint:null`, refund 12000, empty funds and structurally
confirmed terminal metadata. This is **fixture accounting/shape evidence**, not a
claim that any refund transaction or chain observation occurred. Keep this valid
zero-credit path; never invent a final checkpoint merely to satisfy the UI.

## Remaining correction groups

### ER-01 — P1, E4: final source page still claims an undelivered tail

`validateDemoSourcePage`, lines 589–599. The owner test now explicitly asserts
acceptance of the old failure, rather than repairing it.

Minimum regression:

1. Select all coordinator-source events from `createPublicSessionFixture()`.
   Their coordinator IDs reach 4 and host IDs reach 12.
2. Validate the exact page with high_water `{coordinator:'4',research:'0',host:'12'}`
   and `has_more:false`: **must pass**.
3. Change only high_water.host to `'99'`: currently passes; **must reject**.
4. Change has_more to true: **must pass**, retaining high_water 99 without
   implying that the consumer imported it.

The root's actual Chromium seventh assertion reproduces step 3. For a role
present in a final page, its last returned ID must reach the captured high water.
Do not infer the caller's cursor for an absent role or empty page: cross-page
completeness still belongs to L2. This required present-role check uses only
values in the supplied page and does not implement replay history in the helper.

### ER-02 — P2, E6: nested direct input still executes accessors or silently strips fields

`pinSet:453`, `statementCommon`, `sameBytes` and `detached`.

Exact direct-unknown probes:

- Copy the fixture snapshot. Replace `identities.coordinator.agent` with an
  enumerable getter returning the original AgentRef while incrementing a counter.
  `validateDemoSnapshot(copy,pins)` rejects, but the getter executes **once**.
  Expected rejection with **zero reads**. Pin derivation dereferences that field
  before checking the identity object's descriptors.
- Copy a valid authorization event. Replace byte 0 of
  `event.data.credit.payload.purpose` with an enumerable getter returning its
  original integer. `validateDemoEvent` **accepts** and executes it **twice**.
  Expected fixed invalid_event and zero reads; method/purpose comparisons must
  first validate dense data-property arrays, like the other byte fields.
- Attach enumerable `.extra='TEST_FIXTURE_PRIVATE'` to the authorization's
  method byte array. The event is accepted and returned `.extra` is absent.
  Expected rejection, not silent sanitization of a non-JSON descriptor shape.

Keep the now-passing top-level object/array canonical and command getter tests;
extend them to bootstrap extraction and every statement byte-array path. These
are direct API invariants, not claims that JSON on the network can encode a
JavaScript getter. Return fixed codes without leaking a throwing accessor's
message, and validate before reading, spreading, mapping or detaching its values.

### ER-03 — P1, E3/E4: standalone chain observations omit present-field consistency

`localEventData`, lines 547–550. Snapshot economics now checks terminal/refund
relationships, but the source/event branch does not share those necessary checks.

From the fixture pins, construct an ordinary C/H chain_observation with its known
channel, outer request null, `status:'open'`, `redeemed_mist:'0'`,
`locked_mist:'12000'`, `refunded_mist:'100'`, observed_at_ms `'600'`. Each of these
terminal variants currently passes: null; unknown with null digest; confirmed
with canonical nonnull digest. The latter also asserts open and confirmed
terminal state simultaneously. No source history or cryptography is needed to
identify these present-field contradictions.

Expected: reject the contradictory variants; retain the valid open/null-refund/
null-terminal observation and the valid zero-credit confirmed refund. Apply only
relationships derivable from the present fields and immutable known terms. Do
not pretend the helper proved transaction confirmation, signature validity,
freshness or a retained request/credit association. Those remain L1/L2 duties.

### ER-04 — P1, E5: bootstrap compares provider network but not its full namespace

`pinSet:450–460` and `statementCommon`. With no supplied pins, copy the coherent
snapshot and change **only** `identities.provider.agent.package_id` to another
canonical address; repeat changing only `.domain`. Both snapshots are accepted
while their existing offer still names the original common package/deployment.
The old wrong-network variant now correctly rejects.

Expected: validate the complete qualified namespace for both participants of
this demo's common native agreement, then retain it in bootstrap pins. The
actual `NativeChain.resolve` at lines 99–108 checks network/package/domain, not
only bare Agent object ID. Add valid common-namespace and mismatched package/
domain cases both without supplied pins and with consistently changed supplied
provider pins; comparing an invalid bootstrap to itself is not self-consistency.

This is **not** an instruction to equate historical offer keys with current
identity economic keys. Authorized rotation may preserve old agreement rights;
retained epoch/authority association belongs to L1/L2. Current key separation
and the explicit qualified Agent namespace are distinct checks.

### ER-05 — P2, E6: delivered research summaries use the coordinator answer ceiling

`validateEventData`'s coordinator research/follow_up result branch still calls
`text(result.text)` with the default 32,768-byte coordinator-output cap. The
frozen spec instead bounds this duplicate delivered summary by its actual
64-KiB tool-result envelope and the 65,536-byte complete source event.

Nearest-pair structural fixture: copy the public research tool_result, use
plain `x` text of 32,768 then 32,769 bytes, and matching generated/discarded/
delivered-unit count fields. Use a copied pinned policy with output rate zero
to keep the longer output within the unchanged deposit/turn limits. This is an
explicit **synthetic public-shape variant**, not a signed receipt or live delivery
claim. The first case passes; the second rejects invalid_event although its
encoded source event is **33,745 bytes**, tool result **33,394 bytes**, and priced
units are 2 MIST under that variant's policy (turn cap 1000).

Expected: accept both under their own envelope limits; continue rejecting a
32,769-byte **coordinator model_text/operator.task answer** under the default
coordinator limit. Add independently oversized tool-result and source-envelope
negative cases, including escaping overhead. Do not split, truncate, fabricate
delivery, reinterpret reasoning, or expand a real producer's admitted bounds.
L1 still reserves exact public-record headroom before admitting nondefault output.

## E1–E7 disposition and handoff limits

| Original group | Re-review disposition |
|---|---|
| E1 provenance/enums | Exact old failures repaired; 39-row positive/provenance corpus passes |
| E2 unsigned versus signed liability | Independently passes actual ledger reservation, uncertainty and reopen |
| E3 present economics | Exact old snapshot failures repaired; standalone source observation still ER-03 |
| E4 statement/receipt/lifecycle relations | Price, final, receipt bounds, funding and self-cursor cases repaired; ER-01 remains, now reproduced in Chromium |
| E5 detachment/bootstrap | Config detachment, current-key separation, budget, wrong-network and control-pin cases repaired; qualified namespace remains ER-04 |
| E6 pure JSON boundary | Exact basic canonical/control tests repaired; nested descriptors and field-specific delivered-summary bound remain ER-02/05 |
| E7 legal fixed cases | Coordinator backend_unavailable and codec-valid shared refund/payee pass |

The owner suite does not yet establish every nullable lifecycle/transaction state,
all nested unknown-field/privacy classes, every enum scalar mutation, or complete
byte/depth boundary matrices. In particular, its zero-credit test is an open
snapshot, its unsigned-reservation test is handwritten, and its final-page test
explicitly accepts the forbidden tail. The independent actual-ledger and zero-
credit refund probes above supply narrow additional evidence, not production
or exhaustive acceptance.

Return these five groups to Luna with failing permanent regression assertions
before editing. Preserve repaired behavior and the six green browser checks.
Then freeze new hashes for review before helper installation. Do not introduce
stateless signature/hash verification, live authority checks, absent-baseline
inference, cross-page history, snapshot-at-S construction or control permission
decisions to satisfy these structural findings. Native/Move/wire semantics and
the separate real L1/L2/UI integration gates remain unchanged.
